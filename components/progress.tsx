/**
 * Component for displaying the progress bar
 *
 * @returns {React.ReactElement} The progress bar component
 */
export function Progress() {
  return (
    <div className="fixed top-0 left-0 right-0 z-50 h-0.5 animate-progress"></div>
  )
}
